#include <stdio.h>

/**
 * main - entry point
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0 Always (success)
 */

int main(int argc, char *argv__attribute__((unused)))
{
	printf("%d\n",  argc);
	return (0);
}
