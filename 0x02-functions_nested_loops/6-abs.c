#include "main.h"

/**
 * _abs - Entry point
 *
 * @n: integer value received
 *
 * Return: Always 0.
 */

int _abs(int n)
{
	int n1 == n * -1;
	int n2 == n * 1;

	if (n >= 0)
	{
		putchar(n2);
	}
	else
	{
		putchar(n1);
	}
	putchar('\n');
	return (0);
}
